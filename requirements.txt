# 本项目已迁移至 uv 包管理，依赖声明见 pyproject.toml。
#
# 安装方式：
#   uv sync
#
# 如果环境只有 pip（不推荐）：
#   pip install PySide6==6.8.0.2 PySide6-Essentials==6.8.0.2 PySide6-Addons==6.8.0.2 \
#               PyOpenGL==3.1.10 "numpy>=1.24.0,<2.0" "requests>=2.31.0" "pyserial>=3.5"
